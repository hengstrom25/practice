def partlist(arr)
  n = arr.length
  result = []
  for i in 1..n-1 do
    first_part = arr[0, i].join(" ")
    second_part = arr[i..-1].join(" ")
    subresult = [first_part,second_part]
    result.push(subresult)
    end
  result
end
